- API keys are stored in ccredentials.py. Not synced (in .gitignore).
- Use PyCharm global .gitignore for .idea if auto-syncing

ARASAAC API is really responsive.🎉

Consider WordNet. (The one that allegedly already exists from Princeton appears to not exist. Reached out to them directly.)

Consider prompting BLIP-2 with hard-coded questions/directives to refine caption quality. "What kind of locaton is depcited? What actions are taking place?, e.g." Ask about needs. "Does the image contain food?"

RAM++ - this will probably return output that is too dense but check it out.

